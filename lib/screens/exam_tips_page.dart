import 'package:flutter/material.dart';

class ExamTipsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tips to pass the test'),
        backgroundColor: Colors.purpleAccent,
      ),
      body: Container(),
    );
  }
}
